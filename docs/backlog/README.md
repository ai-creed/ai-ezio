# Backlog

Long-horizon, considered-but-not-short-term work. Items here are intentionally
**parked**: candidate future scope that is not scheduled and not committed to a
milestone.

Promote an item out of this folder (into `docs/superpowers/specs/` or an active
plan) only when it is actually picked up for build.
